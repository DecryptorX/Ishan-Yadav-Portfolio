"use client";
import React from 'react';
import Projects from '../../components/Projects';

export default function ProjectsPage() {
  return (
    <div style={{ minHeight: '100vh', padding: '5rem 0 3rem', background: 'var(--bg)' }}>
      <Projects />
    </div>
  );
}
